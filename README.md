# arbor
Arbor is the Shielded State Middleware (SSM), which acts as an abstraction layer between the Solana Virtual Machine (SVM) and the Light Protocol ZK Stack. It manages the transition of data from "Public On-chain State" to "Compressed Private State" using Sparse Binary Merkle Trees.
